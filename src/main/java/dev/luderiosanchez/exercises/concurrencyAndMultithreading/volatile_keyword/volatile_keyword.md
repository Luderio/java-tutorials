# Volatile Keyword

- Another tool for writing thread-safe code without using synchronization
- It provides visibility to fields so that other threads updating on those fields can see it but, it doesn't prevent **race conditions**.